[server]
headless = true
address = "0.0.0.0"
port = 8501
maxUploadSize = 6
# Allow requests from the reverse-proxy host
enableCORS = false
enableXsrfProtection = false

[browser]
# Tell Streamlit the public URL so it generates correct asset/WS links
serverAddress = "ai.graphtrek.co"
serverPort = 443
gatherUsageStats = false
