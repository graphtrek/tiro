# Implementation Plans

Generated by the `/improve` skill on 2026-07-16, planned at commit `56f4d65`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Remove committed secrets from the working tree, close .gitignore gap | P1 | S | — | DONE |
| 002  | Recompute invoice payment status when a tax-account link is cleared | P1 | S | — | DONE |
| 003  | Add tests for bank CSV parsers, fix self-referential tax-guard test | P1 | S–M | — | DONE |
| 004  | Add CI to run every sub-project's test suite | P1 | M | — | DONE (note: full end-to-end verification requires a push to GitHub — not yet done; local dry-runs of invoice-core, attachment-downloader, and auth all passed) |
| 005  | Pagination, N+1 fix, and date-column indexes in invoice-core | P2 | M | — | DONE (note: Part B was reshaped in execution — the N+1 was already fixed by `selectinload(Invoice.bank_transactions)` before this ran, so what shipped instead was a fix to a latent ordering bug in `_partner_invoice_rows` (picked the first-inserted bank transaction instead of the most recently dated one); wiring `vision` to actually paginate through the new `limit`/`offset` params remains a follow-up, not done here) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- All 5 plans are independent and can be executed in any order or in
  parallel. The only soft coordination point: plans 002 and 003 both touch
  `invoice-core/tests/test_matching.py` — if executing both in the same
  session, do 002 first (it only adds a new test function) then 003 (which
  rewrites an existing, different test function in the same file), to
  avoid a merge conflict on that file.
- Plan 004 (CI) will start running plan 003's new bank parser tests and
  plan 002's new regression test automatically once both land — no
  execution-order dependency, just a nice side effect of doing 004 last if
  you want CI to already have something to catch on day one.

## Findings considered and rejected

- **Duplicated `auth.py`/`jwt_auth.py` across 7 services** (invoice-core,
  invoice-file-filter, nav-invoice, attachment-downloader, bank, uploader,
  vision): real duplication, no functional drift found between copies.
  Consolidating into a shared package conflicts with this workspace's
  deliberate "no shared venv, fully isolated per-project" design (see root
  `CLAUDE.md`). Not worth doing unless a concrete bug surfaces in one copy
  that the others don't share.
- **`banking/` as a possibly-abandoned Enable Banking prototype**: confirmed
  no references from any active service, last touched 2026-06-13, likely
  superseded by `bank/`. Recorded as "investigate with the maintainer before
  deleting," not planned as a fix — deletion without confirmation risks
  discarding in-progress work. Folded into the Direction findings (see full
  findings table from the audit session) as part of the "Enable Banking
  integration" option, since the credentials in this directory are already
  being rotated/untracked by plan 001 regardless of whether the code itself
  is kept or removed.

## Findings not yet planned (available for a future round)

These were vetted during the audit but not selected for a plan in this
round — see the full findings table from the audit session for details:

- Scored transaction↔file matcher can auto-link on one weak signal, no
  confidence surfaced (`invoice-core/service.py` `_file_score`/
  `_names_overlap`) — MED effort, needs a product decision on threshold.
- No concurrency guard around `sync_all`; concurrent syncs can hit an
  uncaught `IntegrityError` — MED effort, needs a locking-strategy decision.
- `sync_match` rescans every historical file × every unmatched transaction
  on every run, unbounded by date — MED effort, needs a windowing strategy.
- `service.py` is a 1117-line god-module — MED effort mechanical split.
- Dead `invoice-core/wise_client.py` — S effort, safe to delete anytime.
- Stale docs: CLAUDE.md's `AUTH_ENABLED` description (vision is already
  `true`), `nav-invoice` port default mismatch (8000 vs 8002), dead
  `WISE_URL` in `invoice-core/.env`, missing ruff config despite being
  documented, `banking`/`uploader`/`vault-agent` undocumented in CLAUDE.md.
- `invoice-core` CORS origin hardcoded, not env-driven.
- Direction options: Enable Banking (PSD2) API integration to replace
  manual CSV upload; finishing the `AUTH_ENABLED` rollout across backend
  services now that `vision` has flipped; CSV export for tax/dividend
  reports.
